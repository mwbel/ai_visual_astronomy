# 天文学可视化执行清单 25-1023

本清单用于在现有项目 `ai_visual_astronomy` 基础上，规范化生成一个可交互的 Web 应用“天文学可视化”，并说明运行预览与未来扩展方案。遵循“不改动 src/ 下 Python 源码”的约束，尽量复用其产出的 HTML。

---

## 项目结构树

```text
ai_visual_astronomy/
├─ src/                       # 现有 Python 脚本与可视化生成器（不改动）
├─ app/                       # Web 应用根目录
│  ├─ index.html              # 应用入口（静态路由）
│  ├─ assets/
│  │  └─ js/
│  │     └─ astro_math.js     # 天文计算辅助（如月相粗略计算等）
│  ├─ modules/
│  │  ├─ common/              # 公共 UI 与组件
│  │  │  ├─ styles.css
│  │  │  ├─ SearchBox.js
│  │  │  ├─ LocationSelector.js
│  │  │  ├─ DateSelector.js
│  │  │  └─ BackButton.js
│  │  ├─ modern_astronomy/    # 现代天文学可视化模块
│  │  │  ├─ index.html        # 模块主页（术语卡片 + 搜索）
│  │  │  └─ pages/            # 子页面（可视化页面）
│  │  │     ├─ planetary_daily_motion.html
│  │  │     ├─ planetary_annual_motion.html
│  │  │     ├─ lunar_phase.html
│  │  │     ├─ celestial_center.html
│  │  │     ├─ solar_shadow.html
│  │  │     ├─ twilight_line.html
│  │  │     └─ sundial.html
│  │  └─ tibetan_calendar/    # 藏历历算可视化模块（预留）
│  │     └─ index.html        # 模块主页（占位卡片）
├─ modern-astronomy/index.html  # 静态重定向到模块主页
├─ tibetan-calendar/index.html  # 静态重定向到模块主页
├─ data/
│  └─ cities.csv              # 示例城市库（name, lon, lat, elev_m）
└─ 天文学可视化执行清单25-1023.md
```

---

## 运行 / 预览步骤

- 若需生成或更新某些 Python 产出的 HTML，请先在根目录运行相应脚本（示例）：
  - 计划 → 等待批准：`python src/planetary_daily_motion.py`
  - 计划 → 等待批准：`python src/planetary_annual_motion.py`
  - 计划 → 等待批准：`python src/lunar_phase.py`
- 启动本地静态服务（推荐 Python http.server）：
  - 计划 → 等待批准：`python -m http.server 5173`
- 访问路径（静态服务根指向项目根目录时）：
  - 模块主页：`http://localhost:5173/modern-astronomy/` （重定向至 `app/modules/modern_astronomy/index.html`）
  - 子页面：`http://localhost:5173/modern-astronomy/pages/lunar_phase.html` 等（均在 `app/modules/modern_astronomy/pages/` 下）
- 注意：不改动 `src/` Python 源码；若 Python 产出 HTML 引用本地静态资源，请将资源复制到 `app/assets/` 并修正引用路径。

---

## 模块主页与页面接入

- 模块主页（modern_astronomy/index.html）：
  - 顶部“模块标题 + 搜索框”。
  - 卡片列表（术语名 + 简述）链接到 `pages/` 子页面：
    - 行星实时周日视运动轨迹 → `planetary_daily_motion.html`
    - 行星正午周年视运动轨迹 → `planetary_annual_motion.html`
    - 实时月相 → `lunar_phase.html`
    - 天球球心 → `celestial_center.html`
    - 杆子影长变化 → `solar_shadow.html`
    - 晨昏线 → `twilight_line.html`
    - 日晷可视化 → `sundial.html`
    - 不同纬度的星空可视化 → `latitude_stars.html`

- 每个可视化页面顶部统一一行（顺序固定）：
  - BackButton + LocationSelector + DateSelector
  - 页面内为占位内容或基础可视化（后续替换为 Python 生成或前端实现）。

---

## 组件说明（props / 事件）

- BackButton.js
  - `BackButton.renderBackButton(containerSelector, href?)`
  - 参数：`containerSelector` 容器选择器；`href` 返回链接（默认 `/modern-astronomy/`）。

- LocationSelector.js
  - `LocationSelector.renderLocationSelector(containerSelector, { url })`
  - 参数：`containerSelector` 容器选择器；`url` 城市数据 CSV 路径（如 `/data/cities.csv`）。
  - 行为：选择城市自动回填经度(`lon`)、纬度(`lat`)、海拔(`elev_m`)，并可手动编辑微调。
  - 事件：`location-change`（`CustomEvent`），`detail = { name, lon, lat, elev_m }`。

- DateSelector.js
  - `DateSelector.renderDateSelector(containerSelector, initDate?)`
  - 参数：`containerSelector` 容器选择器；`initDate` 初始日期（可选，默认当前日期）。
  - 事件：`date-change`，`detail = { year, month, day }`。

- SearchBox.js
  - 用于模块主页卡片模糊检索；过滤匹配卡片标题/描述/关键词。
  - 常规初始化：页面内一个搜索输入框（如 `#searchInput`），输入时过滤卡片网格中元素。

---

## 数据说明：data/cities.csv

- 字段规范：`name, lon, lat, elev_m`
- 示例包含城市：上海、北京、拉萨、东京、旧金山、伦敦、开罗、悉尼、布宜诺斯艾利斯。
- 扩展方法：
  - 可替换为完整全球城市库（建议遵守同字段格式）。
  - 确保 `LocationSelector` 读取的路径与文件编码（UTF-8）正确。

---

## 未来扩展：新增历法模块步骤

- 在 `app/modules/` 下新增目录：例如 `lunar_calendar/` 或 `maya_calendar/`。
- 创建 `index.html`（卡片式主页 + 搜索）。
- 在 `app/index.html` 或顶层路由入口追加模块链接（如 `/lunar-calendar/`）。
- 在新模块的 `pages/` 下添加子页面，占位或接入 Python 产出。
- 复用 `common/` 组件与样式，确保统一顶栏与暗色主题。

---

## UI / 配色规范与移动端适配

- 深色主题（夜空配色）：
  - 背景：`#0B0C10`
  - 卡片：`#1F2833`
  - 高亮：`#66FCF1`
  - 按钮：`#45A29E`
  - 文字：白 / 浅灰
- 响应式：卡片网格在窄屏自动变 1 列；顶栏控件在小屏堆叠；交互控件尺寸与间距适配触控。

---

## 依赖清单

- 默认使用原生 Web（无第三方前端框架）。
- 启动本地静态服务：`python -m http.server`（无需额外依赖）。
- 若后续需要引入前端框架（如 React/Vite），请在此处补充 `npm` 安装命令与用途说明，并确保仅在确需时使用。

---

## 已知待办 / 占位说明

- 若某些 `src/` Python 脚本尚未产出对应 HTML：
  - 请先运行脚本生成，再将文件挂载至 `app/modules/modern_astronomy/pages/` 或从卡片链接指向生成结果。
- 部分新页面为占位（`celestial_center.html`, `solar_shadow.html`, `twilight_line.html`, `sundial.html`, `latitude_stars.html`）：
  - 后续将接入具体计算与渲染逻辑（可选 Three.js/Plotly 或原生 Canvas/SVG），并保持统一顶栏与主题。
- 若 Python 产出页面引用静态资源：
  - 将资源拷贝到 `app/assets/` 并修正路径，确保在静态服务下能访问。

---

## 验收点（自检）

- 根目录新增《天文学可视化执行清单25-1023.md》，内容完整。
- 出现 `app/` 目录与两个模块目录；`modern_astronomy/index.html` 在本地静态服务下可访问，展示卡片与搜索。
- 任意一个页面（如 `lunar_phase.html`）能打开，顶部出现 BackButton + LocationSelector + DateSelector。
- LocationSelector 能从 `data/cities.csv` 选择城市并回填经纬/海拔，且可手动编辑。
- 有清晰的“如何把 Python 生成的 HTML 接入/更新”的步骤说明。